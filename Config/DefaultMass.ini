; FogOfWar registers a wrapper for this processor. With no active scene Fog
; actor the wrapper calls the real MassBattleFrame implementation directly;
; with an active actor it executes the filtered visible-work-set path.
[/Script/MassBattle.MassBattleAgentRenderProcessor]
bAutoRegisterWithProcessingPhases=False

; Explicit for auditability. The replacement defaults to the upstream phase,
; priority, public processor name and downstream dependency order.
[/Script/FogOfWar.MassBattleFogAgentRenderProcessor]
bAutoRegisterWithProcessingPhases=True
ActiveWorkSetHalfHeightUU=4096.0
MinimumBatchIdleSeconds=20.0
